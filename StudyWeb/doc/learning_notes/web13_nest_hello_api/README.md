# web13_nest_hello_api

NestJSで `GET /hello` からJSONを返す最小APIです。

## 起動方法

```bash
npm install
npm run start:dev
```

## 確認

```bash
curl http://localhost:3000/hello
```

Controller が受付、Service がレスポンス生成を担当します。
