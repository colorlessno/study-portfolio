# 実行案

## 実行案

固定fixtureを読み、各ロールの模擬成果物を `samples/` に作成し、確認スクリプトで構造を検査する。

## 変更対象

教材内の `samples/` 配下のみを対象にする。

## 実行しない操作

- 外部送信
- ファイル削除
- OS設定変更
- 実秘密情報の参照

## 承認待ち

通常ケースでは承認待ちはない。ファイル変更やコマンド実行を含む依頼では、安全確認で承認待ちにする。
