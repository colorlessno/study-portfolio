# base09 npm scripts

## 目的

`package.json` と `npm scripts` を読み、起動、ビルド、テストの違いを確認します。

## 実行例

```powershell
Set-Location ..\..\..\samples\base09_npm_scripts\sample_node_project
npm run dev
npm run build
npm run test
npm start
```

このサンプルは外部依存なしで動作します。
