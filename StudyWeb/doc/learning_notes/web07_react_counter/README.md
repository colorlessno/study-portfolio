# web07_react_counter

Vite + React + TypeScript で作るカウンターアプリです。

## 起動方法

```bash
npm install
npm run dev
```

## 確認ポイント

- `useState` で `count` を管理している
- ボタンの `onClick` で state を更新している
- state が変わると画面が再描画される
