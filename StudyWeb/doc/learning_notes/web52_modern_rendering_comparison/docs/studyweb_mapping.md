# StudyWeb対応表

| 既存領域 | 関連する表示方式の観点 |
| --- | --- |
| HTTP basics | MPA、SSR の request/response |
| Cookie and session | SSR、MPA、authenticated API calls |
| CORS | SPA が別origin APIを呼ぶ場合 |
| Forms | MPA submit、SPA mutation、SSR action |
| Routing | SPA router と server routes |
| API design | SPA/PWA のdata access |
| File upload | client/server boundary |
| Performance | SSR waterfall、SSG cache、SPA bundle size |

## web52で埋めるgap

既存StudyWebは部品を教えている。`web52` はそれらを表示方式の選定判断へつなげる。
