# Study Hub 将来構想

## 1. 位置付け

この文書は、現在の`StudyXX`群を、将来的に一つの動的な学習管理ツールへ発展させるための構想記録です。

現時点では実装対象にせず、既存教材の整理と学習ノート改善を優先します。実装を開始するときは、この文書を起点に要件定義と技術選定を更新します。

## 2. 目標

静的な教材を探して読むだけの状態から、次の行動を一つの画面で選び、実行し、記録できる状態へ発展させます。

```text
探す
  ↓
再開する
  ↓
動かす・読む・壊して直す
  ↓
理解度と学習記録を残す
  ↓
復習対象と次のテーマを選ぶ
```

仮称は`Study Hub`とします。

## 3. 基本方針

### 3.1 既存教材を一つの巨大アプリへ移動しない

`StudyWeb`、`StudyAI`、`StudySecurity`等は、分野ごとの教材・設計・実装として現在の構成を維持します。Study Hubは、それらを横断して索引化・表示・管理する上位レイヤーとします。

### 3.2 教材定義と個人進捗を分離する

| 情報 | 管理方法 | 例 |
|---|---|---|
| 教材の事実 | Git管理するメタデータ | タイトル、技術、前提、設計・実装パス、起動コマンド |
| 個人の状態 | ローカルDB | 学習状況、最終学習日、理解度、メモ、次の行動 |

教材の構成は再現可能にし、個人の記録は動的に更新できるようにします。

### 3.3 最初は管理に集中する

初期版では「探せる・再開できる・記録できる」を完成させます。npm、Docker、テスト等を画面から起動する機能は、権限・安全性・環境差を整理した後の拡張とします。

### 3.4 各テーマを独立した学習単位として扱う

`web01`、`ai01`等は、StudyAreaのホームページから開けるだけでなく、テーマ単体でも学習を開始・確認・終了できる契約を持たせます。物理ディレクトリや共通サーバーを直ちに分割するのではなく、まず次の入口を揃えます。

| 種別 | 必須の入口 |
|---|---|
| 文書完結型 | 開く文書、15分の再開手順、回答または説明課題、完了条件 |
| 実装型 | 前提条件、起動、動作確認、停止、タイムアウト、安全上の制約 |
| 共通環境利用型 | テーマ指定、共有環境との依存、隔離方法、後片付け |

Study Hubはこの契約を呼び出す上位レイヤーとし、Study Hubがなくても各テーマを直接利用できる状態を保ちます。共通ライブラリや共通Docker環境の共有は許容し、単体確認に必要なテーマ指定と検証入口を分離します。

## 4. 想定する利用者体験

### 4.1 全体ダッシュボード

- `StudyXX`ごとのテーマ数と進捗を表示する
- 最近学習したテーマを表示する
- 長期間開いていないテーマを復習候補として表示する
- 「次にやること」が設定されたテーマを優先表示する
- 未着手、学習中、理解済み、復習対象を絞り込む

### 4.2 テーマ詳細

- 学習目的と到達条件
- 前提テーマと次の候補
- 要件定義、基本設計、詳細設計、実装、テスト、学習ノートへの導線
- 起動コマンドと確認URL
- 「15分で再開する」手順
- 学習履歴、理解度、メモ、次の行動

### 4.3 検索と分類

- 技術タグによる検索
- 分野、難易度、実装形態、成果物種別による絞込み
- `React`、`認証`、`RAG`等をStudyXX横断で検索
- 文書完結型と実装型を区別

## 5. 最小データモデル

| エンティティ | 主な項目 |
|---|---|
| `StudyArea` | id、名前、説明、ルートパス |
| `Theme` | id、番号、タイトル、概要、難易度、種別、状態 |
| `Artifact` | 種別、ファイルパス、説明 |
| `Tag` | 技術名、分野名 |
| `Prerequisite` | 前提Theme、推奨順序 |
| `StudySession` | 学習日時、実施内容、結果、所要時間 |
| `Progress` | 状態、理解度、最終学習日、次の行動 |
| `RunTarget` | 起動・検証・停止コマンド、作業ディレクトリ、確認URL、タイムアウト、安全区分 |

テーマIDは`web09`、`system03`、`security01`等、既存の識別子を維持します。

## 6. 教材メタデータ案

各テーマに`study.yaml`等を置くか、既存文書から生成した中央カタログを使用します。

```yaml
id: web09
area: StudyWeb
title: props / state / list表示
kind: implementation
level: beginner
tags:
  - React
  - TypeScript
artifacts:
  requirements: doc/requirements/web09_props_state_list_requirements.md
  basic_design: doc/basic_design/web09_basic_design.md
  detailed_design: doc/detailed_design/web09_detailed_design.md
  learning_note: doc/learning_notes/web09_props_state_list/README.md
run:
  cwd: src/frontend/src/studyweb/systems/web09_props_state_list
  start: npm run dev
  verify: npm test -- web09
  stop: managed-process
  timeout_seconds: 120
status: available
```

この形式は構想例です。既存の`THEME_CATALOG.md`と重複する情報を手作業で二重管理しないよう、自動生成または検証の方針を先に決めます。

## 7. 推奨アーキテクチャ

```text
StudyXX群
  ↓ metadata scanner
教材カタログ
  ↓
Study Hub Web UI
  ├─ ダッシュボード
  ├─ テーマ詳細
  ├─ 検索・フィルタ
  └─ 学習記録
        ↓
      SQLite
```

初期候補はNext.js + TypeScript + SQLite + Prismaです。既存リポジトリを走査するindexerは、アプリ本体から分離したスクリプトとします。

## 8. 段階的な実装計画

### Phase 0: 現在の土台整備

- 文字化け、壊れたリンク、設計と実装の不一致を修復する
- 学習ノートの共通構成を整える
- テーマの識別子と配置規則を確認する
- 代表テーマから、単体の起動・動作確認・停止または文書課題の入口を揃える
- [StudyXX横断棚卸し](./STUDY_PROJECT_INVENTORY.md)を基準に、各StudyAreaの現在地と改善順序を更新する

### Phase 1: 静的な統合カタログ

- StudyXXを走査してJSONカタログを生成する
- 分野別・タグ別の一覧を表示する
- 各文書と実装へのリンクを一画面に集約する
- DBを使わず、教材の索引として完成させる

### Phase 2: 動的な学習管理

- SQLiteへ進捗と学習セッションを保存する
- 状態、理解度、最終学習日、次の行動を編集できる
- 最近の学習と復習候補をダッシュボードへ表示する
- 教材メタデータはGit、個人記録はDBという責務を分ける

### Phase 3: 実行支援

- 安全と判定したrun targetだけを起動する
- npm、テスト、Docker Compose等の実行ログを表示する
- ポート競合、プロセス終了、タイムアウトを管理する
- 任意コマンドを入力・実行する機能は提供しない

### Phase 4: 学習支援AI

- 学習ノートから確認問題を生成する
- 学習記録を短く要約する
- 苦手タグと未復習テーマを提示する
- AIの判定だけで理解済みにせず、本人の説明・実行記録を優先する

## 9. MVPの受入条件

- 全StudyAreaとテーマを一覧表示できる
- テーマ詳細から設計、実装、学習ノートを開ける
- 状態、理解度、最終学習日、次の行動を保存できる
- 最近のテーマと復習候補を表示できる
- 技術タグとフリーワードで絞り込める
- 教材ファイルが移動・削除された場合に検証で検出できる
- Study Hubがなくても既存教材を直接利用できる

## 10. 主な課題と判断事項

| 課題 | 検討内容 |
|---|---|
| メタデータ重複 | Markdownから抽出するか、YAMLを正とするか |
| テーマ粒度 | StudyXX、番号テーマ、詳細演習の3階層をどう扱うか |
| 進捗の意味 | 読了ではなく、再現・説明・改造等をどう評価するか |
| 実行安全性 | 許可コマンド、作業ディレクトリ、停止方法をどう制限するか |
| 環境差 | Node、Python、Docker、AWS等の前提をどう表示するか |
| データ持運び | SQLiteのバックアップとGit非公開情報をどう分けるか |
| 公開範囲 | ポートフォリオ表示と個人学習記録をどう分離するか |

## 11. 実装開始前に行うこと

1. `web01`、`ai01`等の依存関係を棚卸しし、単体の開始・確認・終了が可能か分類する。
2. 文書完結型、実装型、共通環境利用型の学習単位インターフェースを確定する。
3. 代表テーマで起動・動作確認・停止・タイムアウトを試し、Study Hubなしでも直接利用できることを確認する。
4. Study Hub本体の改修を始める前に、GitHubのレポジトリ名を `study-portfolio` から `study-hub` へ変更する。
5. 名称変更後に、ローカルの`origin`、README、CI、バッジ、文書内リンク、公開URLの参照先を確認して更新する。
6. ユーザー利用シナリオを3〜5件に絞る。
7. 既存テーマを10件だけ選び、確定したインターフェースでメタデータ試作を行う。
8. `THEME_CATALOG.md`との正本関係を決める。
9. DBを使わないPhase 1の画面モックを確認する。
10. 承認後にアプリのディレクトリと技術構成を確定する。

1〜5はStudy Hub改修前の実装ゲートとします。物理ディレクトリ分割は必須条件にせず、単体利用契約と安全な後片付けを満たせるかで判定します。レポジトリ名の変更はこの文書への記録だけでは完了とせず、GitHub上の名称変更と参照先の検証をもって完了とします。

## 12. 現在の判断

- 統合学習ツール化は実現可能で、ポートフォリオと学習継続の両方に有効です。
- 物理的なソース統合ではなく、メタデータと管理画面による論理統合を推奨します。
- `Study`で始まる13ディレクトリを標準のStudyAreaとし、それ以外は明示的に追加します。
- Study Hub改修前に、レポジトリ名を `study-portfolio` から `study-hub` へ変更します。
- 現在はPhase 0を進め、Study Hub本体の実装は将来タスクとします。
