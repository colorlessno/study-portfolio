# Issue: web04でDOM操作の基本を確認できるサンプルを作る

## 背景

HTML、CSS、JavaScriptの役割を分けたうえで、ボタン操作によるDOM変更を練習したい。

## 目的

`addEventListener` を使って、画面上のテキストを変更できるサンプルを作る。

## やること

- [x] `index.html` を作る
- [x] `styles.css` を作る
- [x] `script.js` を作る
- [x] READMEに確認手順を書く

## 完了条件

- [x] ボタン押下で表示テキストが変わる
- [x] リセットで初期表示に戻る
- [x] inline `onclick` を使っていない

## 対象外

フォーム送信、API通信、DB保存は扱わない。

## メモ

DevToolsのConsoleでJavaScriptエラーが出ていないことも確認する。
