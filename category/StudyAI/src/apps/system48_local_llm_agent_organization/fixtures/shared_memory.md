# 共有記憶

## プロジェクト方針

- AIロールは直接会話せず、タスクボードと成果物を通じて連携する。
- ローカルLLMは任意接続とし、未接続時は模擬実行で学習できるようにする。
- 1つのローカルLLMをロールごとに順番に呼び出す。

## 禁止事項

- 実秘密情報、実顧客データ、個人情報を扱わない。
- 外部送信を行わない。
- ファイル削除やOS設定変更をAIが自律実行しない。

## 承認が必要な操作

- ファイル変更
- コマンド実行
- 依存追加
- 長時間実行
