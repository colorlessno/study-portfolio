# base10 curl API確認

## 目的

`curl` で API を直接確認し、フロントエンドに依存せず切り分ける練習です。

## 実行例

```powershell
Set-Location ..\..\..\samples\base10_curl_api_check\sample_api
npm start
```

別ターミナルで `commands/` の curl 例を実行します。
