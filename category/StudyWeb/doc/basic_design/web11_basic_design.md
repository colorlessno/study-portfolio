# web11 基本設計
## TailwindカードUI

---

## 1. システム構成設計

### 1.1 全体構成

```text
Vite Dev Server
  ↓
React App
  ├─ App.tsx
  └─ index.css
      └─ Tailwind CSS
```

### 1.2 コンポーネント一覧

| コンポーネント | 役割 |
|---|---|
| `App.tsx` | カード一覧を表示 |
| `index.css` | Tailwind の読み込みと共通スタイル |
| `tailwind.config.*` | Tailwind の設定 |
| `README.md` | 起動方法と主要クラスを説明 |

---

## 2. 主要設計方針

### 2.1 UI設計方針

- カードにはタイトル、説明、タグ、操作ボタンを配置する
- Tailwind のユーティリティクラスで余白、色、枠線、影を指定する
- `hover:` とレスポンシブクラスを使い、状態変化と画面幅対応を確認する

### 2.2 スタイル設計方針

- 独自CSSは最小限にする
- クラスの羅列が読みにくくなりすぎないよう、UIを小さく分ける
- 色は複数の役割で使い分け、単調な配色にしない

---

## 3. IF仕様

### 3.1 表示IF

| 条件 | 表示 |
|---|---|
| PC幅 | カードを複数列表示 |
| スマートフォン幅 | カードを1列表示 |
| hover | ボタンまたはカードの状態を変化 |

### 3.2 コンポーネントIF

| データ | 用途 |
|---|---|
| カードタイトル | 見出し表示 |
| 説明文 | 本文表示 |
| タグ | 分類表示 |

---

## 4. 処理フロー

```text
npm run dev
  ↓
Vite起動
  ↓
Reactがカード配列を表示
  ↓
Tailwindクラスでスタイル適用
  ↓
画面幅やhoverに応じて見た目が変化
```

---

## 5. データ設計

| データ | 型 | 用途 |
|---|---|---|
| CardItem | `{ title, description, tag }` | カード表示 |

DBは使用しない。

---

## 6. プロンプト・AI制御設計

AI処理は使用しない。

---

## 7. ガードレール・エラー処理設計

- Tailwind が反映されない場合は設定ファイルと CSS 読み込みを確認する
- 長い文字列でカード幅が破綻しないようにする
- レスポンシブ表示で横スクロールが出ないことを確認する

---

## 8. 非機能・運用設計

- Vite + React + TypeScript + Tailwind CSS を使う
- shadcn/ui は使わない
- 起動手順は README に明記する

---

## 9. 技術スタック

| 用途 | 技術 |
|---|---|
| UI | React |
| 型 | TypeScript |
| CSS | Tailwind CSS |
| 開発 | Vite |

---

## 10. 画面一覧

| 画面名 | 目的 | 備考 |
|---|---|---|
| Tailwindカード一覧 | ユーティリティCSSを確認 | `App.tsx` |

---

## 11. 権限制御

認証・認可は扱わない。

---

## 12. 主要導線

| 導線 | 内容 |
|---|---|
| 表示確認 | カード一覧を見る |
| hover確認 | カードやボタンにカーソルを合わせる |
| レスポンシブ確認 | 画面幅を変更する |

---

## 13. 画面遷移図

```text
Tailwindカード一覧
```

---

## 14. 画面項目定義

| 項目 | 種別 | 内容 |
|---|---|---|
| カード | repeated item | タイトル、説明、タグ、ボタン |
| タグ | badge | 分類 |
| 操作ボタン | button | hover確認 |

---

## 15. シーケンス図

```text
学習者 -> ブラウザ: ページ表示
React -> Tailwind: クラス適用
Tailwind -> ブラウザ: レスポンシブ/hoverスタイル適用
```
