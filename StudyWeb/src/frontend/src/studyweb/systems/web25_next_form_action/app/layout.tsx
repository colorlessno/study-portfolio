import "./globals.css";

export const metadata = {
  title: "web25 Form Action",
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="ja">
      <body>{children}</body>
    </html>
  );
}
