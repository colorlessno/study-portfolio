# web11_tailwind_cards

Tailwind CSS でカードUIを作るサンプルです。

## 起動方法

```bash
npm install
npm run dev
```

## 確認ポイント

- `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4` でレスポンシブにしている
- `hover:*` で状態変化を指定している
- 余白、色、枠線、影をTailwindのクラスで指定している
