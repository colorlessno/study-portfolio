# StudyHub application boundary

This directory is reserved for the future learning-management application. The current learning materials do not depend on it: every field remains directly accessible under [`../category/`](../category/), and its verification contract is defined in [`../catalog/fields.json`](../catalog/fields.json).

The management application can be added here without merging or rewriting the individual learning projects.
