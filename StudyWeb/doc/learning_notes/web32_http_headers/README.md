# web32 HTTPヘッダー観察

Node標準HTTPサーバーで、GET/POSTのheader、body、statusを観察するサンプル。

## 実行

```powershell
npm.cmd start
```

確認URL:

- `http://localhost:3032/`
- `http://localhost:3032/api/hello`

curl例は `docs/curl_check.md` を参照。
