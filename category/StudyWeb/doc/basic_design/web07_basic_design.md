# web07 基本設計
## Reactカウンター

---

## 1. システム構成設計

### 1.1 全体構成

```text
ブラウザ
  ↓
Vite Dev Server
  ↓
React App
  └─ App.tsx
      ├─ useState
      └─ onClick handlers
```

### 1.2 コンポーネント一覧

| コンポーネント | 役割 |
|---|---|
| `main.tsx` | ReactアプリをDOMへマウントする |
| `App.tsx` | カウンター状態と操作ボタンを持つ |
| `App.css` | カウンター画面の見た目を定義する |
| `README.md` | 起動方法と state の確認ポイントを説明する |

---

## 2. 主要設計方針

### 2.1 React設計方針

- カウント値は `useState` で管理する
- 加算、減算、リセットはイベントハンドラとして定義する
- state の変更に応じて再描画されることを画面で確認する

### 2.2 UI設計方針

- 現在値を大きく表示する
- 操作ボタンは加算、減算、リセットの3種類にする
- 初学者が処理を追えるよう、コンポーネントは最小構成にする

---

## 3. IF仕様

### 3.1 画面イベントIF

| イベント | 処理 | 出力 |
|---|---|---|
| 加算クリック | `count + 1` | 表示値更新 |
| 減算クリック | `count - 1` | 表示値更新 |
| リセットクリック | `count = 0` | 表示値を0へ戻す |

### 3.2 コンポーネントIF

本サンプルは単一 `App` コンポーネントで構成し、props は使用しない。

---

## 4. 処理フロー

```text
npm run dev
  ↓
Vite Dev Server 起動
  ↓
ブラウザでReactアプリ表示
  ↓
ボタンクリック
  ↓
setCount 実行
  ↓
Reactが再描画
```

---

## 5. データ設計

| データ | 型 | 保持場所 | 用途 |
|---|---|---|---|
| `count` | number | React state | カウント値 |

DBは使用しない。

---

## 6. プロンプト・AI制御設計

AI処理は使用しない。

---

## 7. ガードレール・エラー処理設計

- `useState` の更新は現在値から計算する
- 依存ライブラリが未導入の場合は `npm install` を実行する
- Vite起動ポートが使用中の場合は別ポートで起動する

---

## 8. 非機能・運用設計

- Vite + React + TypeScript を使用する
- 外部状態管理ライブラリは使用しない
- 起動と確認が短時間で完了する構成にする

---

## 9. 技術スタック

| 用途 | 技術 |
|---|---|
| フロントエンド | React |
| 言語 | TypeScript |
| 開発サーバー | Vite |
| スタイル | CSS |

---

## 10. 画面一覧

| 画面名 | 目的 | 備考 |
|---|---|---|
| カウンター画面 | useState と再描画を確認 | `App.tsx` |

---

## 11. 権限制御

認証・認可は扱わない。

---

## 12. 主要導線

| 導線 | 内容 |
|---|---|
| 起動 | `npm run dev` |
| 操作 | 加算、減算、リセット |
| 確認 | state変更による表示更新を見る |

---

## 13. 画面遷移図

```text
カウンター画面
  └─ 同一画面内で count を更新
```

---

## 14. 画面項目定義

| 項目 | 種別 | 内容 |
|---|---|---|
| カウント値 | text | 現在の `count` |
| 加算ボタン | button | +1 |
| 減算ボタン | button | -1 |
| リセットボタン | button | 0に戻す |

---

## 15. シーケンス図

```text
学習者 -> React App: ボタンクリック
React App -> App.tsx: onClick実行
App.tsx -> React State: setCount
React State -> React App: 再描画
React App -> 学習者: 更新値表示
```
