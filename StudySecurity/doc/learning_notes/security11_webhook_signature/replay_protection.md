# リプレイ対策

署名が正しくても、同じeventが再送される場合があります。timestampの許容範囲は古いrequestの受入期間を狭め、event IDの重複検出は同じrequestの再実行を避けます。

本番ではevent IDを共有・永続storeへ保存し、業務処理と処理済み記録の間で片方だけが成功しないようtransaction境界を設計します。保存期間はproviderの最大再送期間より短くしません。
